schema_version: "1"
display_name: "Yeoul Memory"
short_description: "Use Yeoul CLI, agent-pack recipes, and memory rules safely in this repo."
default_prompt: "Use Yeoul proactively: treat Yeoul Core as a memory substrate, not an agent runtime; prefer the user-level database at $HOME/.local/share/yeoul/work-memory.lbug for normal work; search with recipe-backed CLI workflows before recommendations or status interpretation when prior context may matter; check for contradictions before asserting facts; record durable outcomes when they become clear; preserve provenance and lifecycle semantics; and prefer public CLI/API workflows over raw storage queries."
